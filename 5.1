#include <iostream> //Подключение библиотеки iostream
#include <string> //Подключение библиотеки string
#include <math.h> //Подключение библиотеки math.h

using namespace std;
int main(int argc, char* argv[]) //аргументы командной строки
{
  double x1, x2, y1, y2, distance;
    cout <<"Введите координату x1:"<<endl; //Выводим на экран
    cin>>x1; //Вводим с клавиатуры число x1
    cout <<"Введите координату y1:"<<endl; //Выводим на экран
    cin>>y1; //Вводим с клавиатуры число y1
    cout <<"Введите координату x2:"<<endl; //Выводим на экран
    cin>>x2; //Вводим с клавиатуры число x2
    cout <<"Введите координату y2:"<<endl; //Выводим на экран
    cin>>y2; //Вводим с клавиатуры число y2
    distance=sqrt(pow((x2-x1),2)+pow((y2-y1),2)); //Вычисляем расстояние между точками по теореме Пифагора
 cout<<"Расстояние между двумя точками:"<<distance<<endl; //Выводим на экран результат
 }
